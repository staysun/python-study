# 2. 购物系统
    - 个人账户,文件： user,pwd,3,余额
    - 商品，文件
    - 查看商品分页显示：
        li = [
            {...}
            {...}
            {...}
            {...}
            {...}
            {...}
            {...}
        ]
        p = int(input('请输入页码：'))
        start = (p -1) * 10
        end = p * 10
        v1 = li[start:end]
        for i v1:
            print(i)
    - 个人购物记录，文件
        查看：娃娃
    if "al" in "alex":
        pass
"""
1 用户登录
读取user 文件
判断
用户名争取直接登录
读取金额
选择查看
需要读取 购物车文件
读取商品列表 或者购物车
选择商品列表 可以购买商品放入购物车 列表
选择购物车可以继续购买
都买中判断金额 直到没钱为止  可以删除商品
可以有菜单选择 搜索购物车